import { CookieManagementInterceptor } from './cookie-management.interceptor';

describe('CookieManagementInterceptor', () => {
  it('should be defined', () => {
    expect(new CookieManagementInterceptor()).toBeDefined();
  });
});
